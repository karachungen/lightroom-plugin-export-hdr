# CMake / encoder build (FetchContent, nested deps, absolute paths in generated files)
tools/uhdr_repack/build/

# Bundled helper inside plugin (rebuild via scripts/bundle_uhdr_for_plugin.sh)
ExportHDR.lrplugin/bin/uhdr_repack
ExportHDR.lrplugin/bin/*.dylib


# macOS / editor
.DS_Store
*.swp
.vscode/

# Runtime logs if created next to working copy
uhdr_export_*.log

# Generated by scripts/build_release_notes.sh during CI / local dry-runs
RELEASE_NOTES.md
